# 2026-07-16

## 微光集 — 底层架构与产品逻辑确认

- 开启 GitHub Pages：https://elsewherrre.github.io/lightlet/
- 完成五层技术架构设计：View → State → Logic → Storage → Model
  - View: 星夜 Canvas / 脉络 Canvas / 交互组件
  - State: 当前日期 / 当前模式 / 事项缓存
  - Logic: CRUD / 日期查询 / 关联引擎
  - Storage: IndexedDB + localStorage 迁移工具
  - Model: Light / Connection / Day
- 数据模型设计：Light 实体核心字段（id, text, createdAt, completedAt, date, status）
- 产品逻辑确认（家辉决策）：
  - 未完成事项自动带入新一天
  - 历史完全只读
  - 过期时间 11 天，可在设置中自定义
  - 遗忘和归档是同一数据的两种视图：遗忘之海 = 星夜模式看 expired / 档案室 = 脉络模式看 expired
  - 不需要逐个选择遗忘或归档
- 最终 status 三值：pending / done / expired
- 空间结构：今日星空 → 遗忘之海（星夜）/ 今日脉络 → 档案室（脉络）
- 下一步：开始写存储层 + 数据模型 + 业务逻辑 API
- **底层搭建完成**：
  - `js/utils.js` — 日期/ID/HTML 工具
  - `js/db.js` — IndexedDB 封装（CRUD、按日期/状态/范围索引查询）
  - `js/api.js` — 业务逻辑 API（localStorage 迁移、每日滚动、过期检查）
  - `index.html` 已重构接入 API，UI 外观不变，数据层已切换到 IndexedDB
- 已推送到 GitHub，GitHub Pages 自动生效
